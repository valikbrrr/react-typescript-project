const ListItem = () => {
  return (
    <li>
      tegliiiii
      {/* className="col-start-3 col-span-2 my-2 py-1 border-2 border-black px-2 grid grid-flow-row grid-cols-12"> */}
      {/* <input
        type="checkbox"
        className="col-span-1"
        checked={complited}
        onChange={() => dispatch(toggleTodo({ id }))}
      />
      <span className="col-span-10 overflow-auto px-1">{text}</span>
      <div
        className="col-span-1 cursor-pointer"
        onClick={() => dispatch(removeTodo({ id }))}
      >
        <XCircle />
      </div> */}
    </li>
  );
};

export default ListItem;
